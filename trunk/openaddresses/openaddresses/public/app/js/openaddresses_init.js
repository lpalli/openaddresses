
/*
 * @include openaddresses_layout.js
 */

Ext.namespace("openaddresses");

(function() {
    // run openaddresses.layout.init() when the page
    // is ready
    Ext.onReady(function() {
        openaddresses.layout.init()
    });
})();
